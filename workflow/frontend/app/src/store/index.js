import { createStore } from 'vuex'

//I have tried to use this
//But I have failed
//I will now proceed to use localStorage for everything
//I am at my wit's end...

export default createStore({
  //state is data
  state: {
  },
  //getters are computed (depend on data)
  getters: {
  },
  //used to directly modify data
  mutations: {
  },
  //calls on mutations
  actions: {
  }
})
